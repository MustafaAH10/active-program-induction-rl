import{describe,expect,it}from'vitest';import{assertDag}from'@skyfoundry/construction-engine';import{createManhattanDemo}from'./index';
describe('Manhattan fixture',()=>{it('is deeply deterministic and exercises the required systems',()=>{const a=createManhattanDemo(),b=createManhattanDemo();expect(a).toEqual(b);expect(a.scene.components).toHaveLength(1764);expect(new Set(a.scene.components.map(x=>x.id)).size).toBe(a.scene.components.length);expect(a.scene.agentRounds.length).toBeGreaterThanOrEqual(6);expect(new Set(a.scene.tasks.map(x=>x.phase)).size).toBe(10);expect(assertDag(a.scene.tasks)).toBe(true);for(const type of ['core_segment','column','beam','slab','curtain_wall_panel','tower_crane','worker','excavator','scaffold','planter'])expect(a.scene.components.some(c=>c.type===type),`missing ${type}`).toBe(true)})});
