from __future__ import annotations

import argparse
import json
from pathlib import Path
from statistics import mean

from active_program_induction.dataset import read_jsonl
from active_program_induction.prompts import active_prompt
from active_program_induction.scoring import score_completion


def main() -> None:
    parser = argparse.ArgumentParser(description="Run base-model cold-start evaluation.")
    parser.add_argument("--model", required=True, help="Hugging Face model id or local path")
    parser.add_argument("--tasks", required=True, help="task JSONL generated by api-bench")
    parser.add_argument("--out", default="data/generated/cold_start_results.jsonl")
    parser.add_argument("--limit", type=int, default=50)
    parser.add_argument("--max-new-tokens", type=int, default=1536)
    parser.add_argument("--temperature", type=float, default=0.7)
    parser.add_argument("--top-p", type=float, default=0.95)
    parser.add_argument("--dtype", default="auto")
    args = parser.parse_args()

    try:
        import torch
        from transformers import AutoModelForCausalLM, AutoTokenizer
    except ImportError as exc:
        raise SystemExit("Install GPU extras first: python3 -m pip install -e '.[gpu]'") from exc

    tokenizer = AutoTokenizer.from_pretrained(args.model, trust_remote_code=True)
    model = AutoModelForCausalLM.from_pretrained(
        args.model,
        torch_dtype=args.dtype,
        device_map="auto",
        trust_remote_code=True,
    )
    tasks = read_jsonl(args.tasks)[: args.limit]
    Path(args.out).parent.mkdir(parents=True, exist_ok=True)

    rewards = []
    correct = 0
    with Path(args.out).open("w", encoding="utf-8") as f:
        for task in tasks:
            prompt = active_prompt(task)
            inputs = tokenizer(prompt, return_tensors="pt").to(model.device)
            with torch.no_grad():
                output = model.generate(
                    **inputs,
                    do_sample=args.temperature > 0,
                    temperature=args.temperature,
                    top_p=args.top_p,
                    max_new_tokens=args.max_new_tokens,
                    pad_token_id=tokenizer.eos_token_id,
                )
            completion = tokenizer.decode(
                output[0][inputs["input_ids"].shape[-1] :],
                skip_special_tokens=True,
            )
            scored = score_completion(task, completion, mode="graded")
            rewards.append(float(scored["reward"]))
            correct += int(bool(scored["equivalent"]))
            row = {
                "task_id": task["task_id"],
                "family": task["family"],
                "tier": task["tier"],
                "reward": scored["reward"],
                "equivalent": scored["equivalent"],
                "num_queries": scored["num_queries"],
                "completion": completion,
                "score_detail": scored,
            }
            f.write(json.dumps(row, sort_keys=True) + "\n")
            print(json.dumps({k: row[k] for k in ("task_id", "reward", "equivalent")}))

    summary = {
        "n": len(rewards),
        "mean_reward": mean(rewards) if rewards else 0.0,
        "min_reward": min(rewards) if rewards else 0.0,
        "max_reward": max(rewards) if rewards else 0.0,
        "pass_rate": correct / len(rewards) if rewards else 0.0,
        "out": args.out,
    }
    print(json.dumps(summary, indent=2, sort_keys=True))


if __name__ == "__main__":
    main()
